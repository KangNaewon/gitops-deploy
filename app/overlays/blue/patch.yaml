apiVersion: apps/v1
kind: Deployment
metadata:
  name: fastapi-todo
spec:
  template:
    metadata:
      labels:
        version: blue
    spec:
      containers:
      - name: fastapi
        image: knw125/fastapi-app:v20

